val name = "Jason Isaacs"
println(s"Hello to $name!")

s"The answer is ${6 * 7}."

// The raw string interpolator
println(raw"No\\\\escape\n")

// The f string interpolator
f"${math.Pi}%.5f"

val pi = "Pi"
println(f"$pi is approximately ${math.Pi}%.6f")